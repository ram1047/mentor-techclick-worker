{
  "name": "mentor-techclick-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Techclick Mentor Desk — mentor.techclick.in",
  "scripts": {
    "check": "node --check src/index.js && node --check src/validate.js && node --check public/app.js && node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
